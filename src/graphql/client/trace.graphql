
query GetTraceById($id: Int!) {
  trace(id: $id) {
    id
    traceType
    website {
      id
      name
      url
      pingInterval
    }
    duration
    status
    requestHeaders
    responseHeaders
    responseData
    createdAt
  }
}

mutation CreateTrace($trace: CreateTrace!) {
  createTrace(trace: $trace) {
    id
    traceType
    websiteId
    duration
    status
    requestHeaders
    responseHeaders
    responseData
  }
}

query TraceOfErrorCount($rangeTime: String!, $websiteId: Int) {
  traceOfErrorCount(rangeTime: $rangeTime, websiteId: $websiteId) {
    time
    groupId
    count
  }
}

query TraceOfResponseTime($rangeTime: String!) {
  traceOfResponseTime(rangeTime: $rangeTime) {
    time
    groupId
    avgDuration
  }
}

query Traces($query: TraceQuery!) {
  traces(query: $query) {
    minId
    maxId
    results {
      id
      traceType
      websiteId
      duration
      status
      requestHeaders
      responseHeaders
      responseData
      website {
        id
        name
        url
      }
    }
  }
}